---
name: Molecule
# yamllint disable-line rule:truthy
on:
  pull_request:
    paths-ignore:
      - '!.github/workflows/**'
      - '**.cfg'
      - '**.md'
      - .*
      - LICENSE
      - Pipfile*
  push:
    paths-ignore:
      - '!.github/workflows/**'
      - '**.cfg'
      - '**.md'
      - .*
      - LICENSE
      - Pipfile*
  workflow_dispatch:
jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        scenario:
          - default
    steps:
      - uses: actions/checkout@v6
        with:
          path: ${{ github.repository }}
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          cache: pip
          cache-dependency-path: |
            ${{ github.repository }}/.devcontainer/requirements.txt
            ${{ github.repository }}/Pipfile*
          python-version: '3.12'
      - name: Install Python's requirements
        run: pip install -q -r .devcontainer/requirements.txt
        working-directory: ${{ github.repository }}
      - name: Install Ansible Galaxy requirements
        run: ansible-galaxy collection install -r requirements.yml -p collections
        working-directory: ${{ github.repository }}
      - uses: gofrolist/molecule-action@v2
        env:
          ANSIBLE_FORCE_COLOR: '1'
          ANSIBLE_STDOUT_CALLBACK: ${{ runner.debug == '1' && 'debug' || 'community.general.unixy' }}
          ANSIBLE_COLLECTIONS_PATH: ${{ github.workspace }}/${{ github.repository }}/collections:${{ github.workspace }}/${{ github.repository }}
          ANSIBLE_DISPLAY_SKIPPED_HOSTS: 'false'
          ANSIBLE_DISPLAY_OK_HOSTS: 'false'
          ANSIBLE_CALLBACK_RESULT_FORMAT: yaml
          MOLECULE_REPORT: molecule-${{ matrix.scenario }}-report.html
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          molecule_args: >-
            --scenario-name ${{ matrix.scenario }}
          molecule_command: test
          molecule_options: ${{ runner.debug == '1' && '--verbose' || '' }}
          molecule_working_dir: ${{ github.repository }}/roles/github_models
      - if: always()
        name: Ensure report file permissions are correct
        run: sudo chmod 644 molecule-${{ matrix.scenario }}-report.html
        working-directory: ${{ github.repository }}/roles/github_models
      - if: always()
        name: Converts test report
        uses: patrick-werner/fhir-html-validation-to-markdown@1.0.0
        with:
          input_file: ${{ github.repository }}/roles/github_models/molecule-${{ matrix.scenario }}-report.html
      # Debug with tmate on failure when debug logging is enabled.
      - if: ${{ failure() && runner.debug == '1' }}
        uses: mxschmitt/action-tmate@v3
    timeout-minutes: 20
