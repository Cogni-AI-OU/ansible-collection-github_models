---
- name: Configure Docker container
  # Note: Since facts are not gathered, we're checking for ansible_facts['user_id'], connection_user or UID instead.
  become: >-
    {{ (connection_user is defined and connection_user != 'root')
    or (ansible_facts['user_id'] | default('root') != 'root')
    or (lookup('env', 'UID') | int != 0)
    }}
  # Use su as become_method as sudo is not available in all containers.
  become_method: ansible.builtin.su
  gather_facts: false
  hosts: docker_containers
  pre_tasks:
    - name: Render Dockerfile for NixOS image
      ansible.builtin.template:
        src: '{{ nixos_dockerfile_template }}'
        dest: '{{ nixos_dockerfile_output }}'
        mode: '0644'
      delegate_to: localhost
      run_once: true  # noqa: run-once[task]
      when: "'github_models-on-nixos-latest' in groups['docker_containers']"
      vars:
        item:
          name: github_models-on-nixos-latest
          image: nixos/nix:latest
        ansible_python_interpreter: '{{ controller_python }}'
    - name: Build NixOS image with Python preinstalled
      community.docker.docker_image:
        name: '{{ nixos_built_image }}'
        build:
          path: '{{ nixos_dockerfile_output | dirname }}'
          dockerfile: '{{ nixos_dockerfile_output | basename }}'
        source: build
        force_source: true
      delegate_to: localhost
      run_once: true  # noqa: run-once[task]
      when: "'github_models-on-nixos-latest' in groups['docker_containers']"
      vars:
        ansible_python_interpreter: '{{ controller_python }}'
    - name: Ensure container is started
      community.docker.docker_container:
        name: '{{ inventory_hostname }}'
        image: '{{ container_image }}'
        state: started
        command: sleep infinity
        # NixOS images require privileged mode due to their non-standard filesystem layout
        # and init expectations when run under Docker, especially in CI environments.
        privileged: "{{ container_image is search('nixos') }}"
        # Relaxed seccomp/apparmor for NixOS compatibility.
        security_opts: >-
          {{ ['seccomp=unconfined', 'apparmor=unconfined']
          if container_image is search('nixos') else omit }}
        recreate: true
        pull: >-
          {{ not (container_image is search('cogni-ai-github_models-nixos')) }}
      delegate_to: localhost
      vars:
        # Use system Python with requests installed for controller-side Docker modules.
        ansible_python_interpreter: '{{ controller_python }}'
    - name: Wait for container to be ready
      community.docker.docker_container_info:
        name: '{{ inventory_hostname }}'
      register: container_info
      until:
        - container_info is defined
        - container_info.container.State.Running | default(false) | bool
      changed_when: false
      delay: 2
      delegate_to: localhost
      retries: 10
      vars:
        # Use system Python with requests installed for controller-side Docker modules.
        ansible_python_interpreter: '{{ controller_python }}'
    - name: Install Python 3 on Alpine
      ansible.builtin.raw: |
        apk update -q && apk add -q --no-cache python3 py3-pip py3-packaging
      changed_when: false
      when: container_image is search("alpine")
    - name: Install Python 3 on Ubuntu/Debian
      ansible.builtin.raw: |
        apt-get update -y -qq && apt-get install -y -qq python3 python3-pip python3-packaging
      changed_when: false
      when: container_image is search("ubuntu") or container_image is search("debian")
    - name: Gather facts
      ansible.builtin.setup:
    - name: Install Python packages for Ubuntu/Debian
      ansible.builtin.apt:
        name:
          - python3
          - python3-pip
          - python3-setuptools
          - python3-packaging
        state: present
        update_cache: true
      when: container_image is search("ubuntu") or container_image is search("debian")
      changed_when: false
      no_log: true
    - name: Install azure-ai-inference on all containers
      ansible.builtin.pip:
        name: azure-ai-inference
        state: present
        break_system_packages: true
      no_log: true
  tags: always
  vars:
    controller_python: '{{ ansible_playbook_python }}'
    nixos_built_image: cogni-ai-github_models-nixos:latest
    nixos_dockerfile_output: >-
      {{ playbook_dir }}/../../roles/github_models/molecule/resources/playbooks/Dockerfile.github_models-on-nixos
    nixos_dockerfile_template: >-
      {{ playbook_dir }}/../../roles/github_models/molecule/resources/playbooks/Dockerfile.j2
    container_image_map:
      github_models-on-alpine-latest: alpine:latest
      github_models-on-debian-latest: debian:latest
      github_models-on-nixos-latest: '{{ nixos_built_image }}'
      github_models-on-ubuntu-jammy: ubuntu:jammy
      github_models-on-ubuntu-latest: ubuntu:latest
      github_models-on-ubuntu-noble: ubuntu:noble
    container_image: "{{ container_image_map[inventory_hostname] | default('ubuntu:latest') }}"
- name: Install cogni-ai.github_models role on all containers
  hosts: docker_containers
  gather_facts: true
  tags: always
  vars:
    controller_python: '{{ ansible_playbook_python }}'
  tasks:
    - name: Installs cogni-ai.github_models role
      ansible.builtin.import_role:
        name: cogni_ai.github_models.github_models
  post_tasks:
    - name: Stop Docker containers
      community.docker.docker_container:
        name: '{{ inventory_hostname }}'
        state: stopped
      delegate_to: localhost
      failed_when: false
